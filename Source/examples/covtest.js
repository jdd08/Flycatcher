
var a = Math.floor(Math.random()*2);
if(a == 0) {
    //print("if");
    var ab = Math.floor(Math.random()*2);
    if(ab == 0) {
        //print("if");
        var ac = Math.floor(Math.random()*2);
        if(ac == 0) {
            //print("if");
            var ad = Math.floor(Math.random()*2);
        }
        else {
            //print("else");
            var nad = Math.floor(Math.random()*2);
        }
    }
    else {
        //print("else");
        var ac = Math.floor(Math.random()*2);
        if(ac == 0) {
            //print("if");
            var ad = Math.floor(Math.random()*2);
        }
        else {
            //print("else");
            var nad = Math.floor(Math.random()*2);
        }
    }
}
else {
    //print("else");
    var ab = Math.floor(Math.random()*2);
    if(ab == 0) {
        //print("if");
        var ac = Math.floor(Math.random()*2);
        if(ac == 0) {
            //print("if");
            var ad = Math.floor(Math.random()*2);
        }
        else {
            //print("else");
            var nad = Math.floor(Math.random()*2);
        }
    }
    else {
        //print("else");
        var ac = Math.floor(Math.random()*2);
        if(ac == 0) {
            //print("if");
            var ad = Math.floor(Math.random()*2);
        }
        else {
            //print("else");
            var nad = Math.floor(Math.random()*2);
        }
    }
}